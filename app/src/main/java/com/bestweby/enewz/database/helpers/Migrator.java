package com.bestweby.enewz.database.helpers;


class Migrator {

/*    static final Migration MIGRATION_1_2 = new Migration(1, 2) {
        @Override
        public void migrate(SupportSQLiteDatabase database) {
            database.execSQL("ALTER TABLE " + DaoHelper.FAVOURITE_PRODUCT_TBL + " ADD COLUMN " + DaoHelper.COLUMN_PRICE_CURRENCY + " TEXT");
            database.execSQL("ALTER TABLE " + DaoHelper.PRODUCT_CART_TBL + " ADD COLUMN " + DaoHelper.COLUMN_PRICE_CURRENCY + " TEXT");
        }
    };*/

/*    static final Migration MIGRATION_2_3 = new Migration(2, 3) {
        @Override
        public void migrate(SupportSQLiteDatabase database) {
            database.execSQL("ALTER TABLE " + DaoHelper.PRODUCT_CART_TBL + " ADD COLUMN " + DaoHelper.COLUMN_PRICE_CURRENCY);
        }
    };*/

}